{
  "name": "ernestoacher.cl",
  "version": "2.0.0",
  "description": "Sitio web de Ernesto Acher",
  "main": "index.js",
  "scripts": {
    "sass:build": "sass scss:css --style=compressed --no-source-map",
    "sass:watch": "sass --watch scss:css --style=expanded --source-map",
    "build": "npm run sass:build",
    "dev:serve": "python3 -m http.server 3012",
    "dev": "npm run build && npm run dev:serve"
  },
  "keywords": [
    "ernesto-acher"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "live-server": "^1.2.2",
    "npm-run-all": "^4.1.5",
    "sass": "^1.97.2"
  }
}